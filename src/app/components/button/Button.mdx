import { Meta, Controls, Stories} from '@storybook/blocks'
import * as Button from './button.stories'

<Meta of={Button} title='Button' />

# Button

A button component contains a two variants:
  - Primary: The primary button contains a simple text label.
  - Icon: The icon button contains an icon and a text label.

## Inputs

The component accepts the following inputs (props):

<Controls />

<Stories />
